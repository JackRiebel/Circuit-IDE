# Retired Python Circuit IDE

This Python GUI, TUI, CLI, and test suite are archived from the pre-Flutter
Circuit IDE. They are intentionally not installed, packaged, launched, or
validated by current CircuitCode CI.

Do not use these modules for new features, releases, credentials, or security
claims. The maintained product is `../../circuit-ide/`.
